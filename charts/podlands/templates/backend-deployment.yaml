apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "podlands.name" . }}-backend
  namespace: {{ .Values.namespace }}
  labels:
    {{ .Values.backend.labels | toYaml | nindent 4 }}
  annotations:
    checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
spec:
  {{- if not .Values.backend.deployment.autoscaling.enabled }}
  replicas: {{ .Values.backend.deployment.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{ .Values.backend.labels | toYaml | nindent 6 }}
  template:
    metadata:
      {{- with .Values.backend.deployment.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{ .Values.backend.labels | toYaml | nindent 8 }}
    spec:
      {{- with .Values.backend.deployment.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ .Values.backend.deployment.serviceAccount.name }}
      {{- with .Values.backend.deployment.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Values.backend.name }}
          {{- with .Values.backend.deployment.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "podlands.name" . }}-config
          ports:
            - name: http
              containerPort: {{ .Values.backend.service.port }}
              protocol: TCP
          {{- with .Values.backend.deployment.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.deployment.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.deployment.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}