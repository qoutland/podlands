apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podlands
  namespace: podlands
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/podlands
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  values:
    backend:
      image:
        repository: ghcr.io/qoutland/podlands/backend
        tag: "2025.1223.2124" # {"$imagepolicy": "flux-system:podlands-backend:tag"}
    frontend:
      image:
        repository: ghcr.io/qoutland/podlands/frontend
        tag: "2025.1223.2127" # {"$imagepolicy": "flux-system:podlands-frontend:tag"}
  snake:
    image:
      repository: ghcr.io/qoutland/podlands/snake
      tag: "2025.1218.200" # {"$imagepolicy": "flux-system:podlands-snake:tag"}